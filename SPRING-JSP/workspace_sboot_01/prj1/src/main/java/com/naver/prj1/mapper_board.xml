<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.naver.prj1.BoardDAO">
<select id="getboardList" parameterType="int" resultType="Map">
    select e.*
    from (select rownum "RNUM", b.* from board b where rownum <= {pageNum*10}}) e
    where e.RNUM >= {(pageNum*10)/10}
</select>
    <insert id="insertBoard" parameterType="com.naver.prj1.BoardDTO">
            insert into board(
            b_no,
            subject,
            writer,
            readcount,
            content,
            pwd,
            email,
            group_no,
            print_no,
            print_level
        ) values(
            (select nvl(max(b_no),0)+1 from board), 
            #{subject},
            #{writer}, 
            0,            
            #{content}, 
            #{pwd}, 
            #{email}, 
            (select nvl(max(b_no),0)+1 from board), 
            0,
            0
        )
    </insert>
</mapper>